const placeholderStyle = 'placeholder:text-neutral-dark-100'
const border = 'border-solid border border-neutral-dark-250 rounded'
const focus = 'focus:drop-shadow-input'
const standard = `p-4 w-full ${placeholderStyle} ${border} ${focus}`

export { placeholderStyle, border, focus, standard }
